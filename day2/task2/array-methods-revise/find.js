// find - iterates each element, returns the first element from an array. if no one condition statify then it return undefined.

//Qn - find first even number
const numbers = [3, 7, 10, 15, 20];

const ans = numbers.find((num) => num % 2 == 0)
console.log(ans)

