export default class Cards {
    constructor(scene) {
        this.scene = scene;
        this.create();
    }

    create() {
        // new Cards(this.scene, 100, 700)
        //  new Cards(this.scene, 100, 700, "A.png", "1.98", "0/1K");
        // this.cards = this.scene.add.image(100,700, "A.png", "0/1K");
        // this.cardImage = this.scene.add.image(100, 700, "cardAtlas", "A.png")
        // this.betText   = this.scene.add.text(100, 780, "0/1K", {
        // fontSize: "20px",
        // color: "#ffffff"
        // }).setOrigin(0.5)
    }
}